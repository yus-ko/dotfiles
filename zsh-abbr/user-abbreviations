abbr "add_serena"="claude mcp add serena -- uvx --from git+https://github.com/oraios/serena serena start-mcp-server --context claude-code --project \$(pwd)"
abbr "bat"="batcat"
abbr "cat"="batcat"
abbr "build"="docker compose up -d --build"
abbr "c"="z \$(fd --type d | fzf)"
abbr "cd"="z"
abbr "df"="duf"
abbr "down"="docker compose down"
abbr "du"="dust"
abbr "find"="fd"
abbr "gitlog"="git log --graph --oneline --all"
abbr "h"="print -z -- \$(history -nr 1 | fzf)"
abbr "la"="eza --icons -A"
abbr "ldo"="lazydocker"
abbr "lg"="lazygit"
abbr "ll"="eza --icons -lAh"
abbr "ls"="eza --icons"
abbr "ps"="procs"
abbr "remove_serena"="claude mcp remove serena"
abbr "restart"="docker compose restart"
abbr "top"="btm"
abbr "up"="docker compose up -d"
abbr "vim"="nvim"
