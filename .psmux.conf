# psmux configuration for native Windows sessions.
# Keep key bindings in .tmux.conf as the shared source of truth.

source-file "~/.tmux.conf"

set -g default-shell "bash.exe"
set -g default-command "bash.exe --login -i"

bind r source-file ~/.psmux.conf \; display "Reloaded psmux!"
